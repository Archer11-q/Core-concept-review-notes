进程与线程的区别（资源、调度、开销、通信）
1.资源：
进程：资源分配的最小单位，有独立的地址空间、独立文件、独立内存
线程：共享进程的资源（内存、文件句柄等），只独有栈、寄存器

2.调度：
进程：调度的基本单位，但切换成本高
线程：CPU调度的最小单位，同一个进程内线程切换极快

3.开销：
进程：创建、销毁、切换开销大
线程：创建、销毁、切换开销很小

4.通信：
进程：之间必须用IPC（管道、消息队列、共享内存等）
线程：之间直接共享全局变量/内存，通信最简单，但要注意线程安全
